---
- name: Configure NFS Server
  hosts: nfs_server
  gather_facts: false
  tags: [ 'nfs' ]
  roles:
    - 'nfs-server'

- name: Configure NFS Clients
  hosts: nodes
  gather_facts: false
  tags: [ 'nfs' ]
  roles:
    - 'nfs-client'

- name: Configure KVM
  hosts: nodes
  gather_facts: false
  tags: [ 'nfs' ]
  roles:
    - 'kvm-qemu'
